#include<stdio.h>
int main()
{
    float r,x;
printf("Enter the radius of the circle:",r);
scanf("%f",&r);
x=2*3.14*r;
printf("Area of circle is %.2f sq. unit having the radius %.0f unit.",x,r);
return 0;
}